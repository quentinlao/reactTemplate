import ReactDOM from 'react-dom';
import React from 'react';
console.log('🚀 ~ file: index.tsx ~ line 7 ~ template');
const App = (): JSX.Element => {
    return <h1>React App template</h1>;
};

ReactDOM.render(<App />, document.getElementById('app'));
